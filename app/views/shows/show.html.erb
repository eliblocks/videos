<p class="title"><%= @show.title %></p>
<% @sections.each do |section| %>
  <div class="columns">
    <div class="column list-item">
      <%= link_to section_path(section) do %>
        <div class="card">
          <div class="card-content">
            <p><%= section.title || "Section X" %></p>
          </div>
        </div>
      <% end %>
    </div>
  </div>
<% end %>    